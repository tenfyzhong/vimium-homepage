{
  "name": "vimium-homepage",
  "version": "1.0.0",
  "private": true,
  "description": "Vimium Homepage: dark terminal new tab page embedding the Vimium Vomnibar.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
