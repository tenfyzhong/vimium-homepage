{
  "name": "vimium-homepage",
  "version": "1.0.0",
  "private": true,
  "description": "Vimium Homepage: system-themed new tab page for Vimium users (native Vomnibar, press o).",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
